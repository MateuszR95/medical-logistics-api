<?xml version="1.0" encoding="UTF-8"?>
<databaseChangeLog
        xmlns="http://www.liquibase.org/xml/ns/dbchangelog"
        xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
        xsi:schemaLocation="http://www.liquibase.org/xml/ns/dbchangelog
        https://www.liquibase.org/xml/ns/dbchangelog/dbchangelog-4.2.xsd">

    <changeSet id="009_create_location_table" author="mrojek">
        <createTable tableName="location">
            <column name="id" type="bigint" autoIncrement="true">
                <constraints nullable="false" primaryKey="true"/>
            </column>
            <column name="warehouse_id" type="bigint">
                <constraints nullable="false"/>
            </column>
            <column name="code" type="varchar(40)">
                <constraints nullable="false"/>
            </column>
            <column name="zone" type="varchar(30)">
                <constraints nullable="false"/>
            </column>
            <column name="active" type="boolean">
                <constraints nullable="false"/>
            </column>
        </createTable>

        <addUniqueConstraint
                tableName="location"
                columnNames="warehouse_id, code"
                constraintName="uq_location_warehouse_code"/>

        <addForeignKeyConstraint
                baseTableName="location"
                baseColumnNames="warehouse_id"
                referencedTableName="warehouse"
                referencedColumnNames="id"
                constraintName="fk_location_warehouse"/>
    </changeSet>

</databaseChangeLog>
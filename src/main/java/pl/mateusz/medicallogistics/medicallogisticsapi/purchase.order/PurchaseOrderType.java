package pl.mateusz.medicallogistics.medicallogisticsapi.purchase.order;

/**
 * Enum representing the type of purchase order.
 */
public enum PurchaseOrderType {

  FROM_CONSIGNMENT,
  FROM_LOANER_SET
}

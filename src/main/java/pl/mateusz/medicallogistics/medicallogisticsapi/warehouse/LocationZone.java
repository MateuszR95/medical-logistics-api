package pl.mateusz.medicallogistics.medicallogisticsapi.warehouse;

/**
 * Enum representing different zones within a warehouse location.
 */
public enum LocationZone {
  RECEIVING_REPACK,
  STORAGE,
  ALLOCATED,
  QUARANTINE,
  RETURNS
}
